<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">

	<!-- Interceptor Start -->
	<!-- Interceptor for 'locale change' with bean for locale change interceptor -->
	<mvc:interceptors>
		<bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor" p:paramName="language"></bean>
	</mvc:interceptors>
	<!-- Locale resolver (in our example we have two locales (english and spanish)-->
	<bean id="localeResolver" class="org.springframework.web.servlet.i18n.SessionLocaleResolver" p:defaultLocale="en"></bean>
	<!-- Interceptor End -->

	<!-- This tag is to recognize static files/resources in the project -->
	<mvc:resources location="/PDFs/" mapping="/PDFs/**"/>

	<!-- These 2 tags are for recognizing annotation based controllers in the project -->
	<mvc:annotation-driven/>
	<context:component-scan base-package="org.pluralsight.controller"/>

	<!-- This bean is for identifying the resource related properties file (in our case - messages.properties) -->
	<bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource" p:basename="messages"></bean>

	<!-- This bean is for recognizing JSPs located in the project (new way by using the 'p' tag  -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver" 
		  p:suffix=".jsp" 
		  p:prefix="/WEB-INF/jsp/"></bean>
		  
	<!-- This bean is for recognizing JSPs located in the project (old style)-->	  
	<!-- 
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/"/>
		<property name="suffix" value=".jsp"/>
	</bean>
	 -->


</beans>
